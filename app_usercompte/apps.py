from django.apps import AppConfig

class AppUsercompteConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'app_usercompte'  # ce nom DOIT correspondre exactement au nom du dossier
